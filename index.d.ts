/**
 * Loads an image.
 * @param url The URL to load.
 * @returns The loaded image.
 */
export declare function load(url: string): Promise<ImageBitmapSource>;
/**
 * Loads an ImageBitmap.
 * @param url The URL to load.
 * @returns The loaded ImageBitmap.
 */
export declare function loadBitmap(url: string): Promise<ImageBitmap>;
